# XFinder-CTEM Integrated Scanner — Dependencies
# Install: pip install -r requirements.txt

# Supabase Python client (for CTEM database sync)
supabase>=2.0.0

# Note: All scanner functionality uses Python standard library only.
# External scanning tools must be installed separately (see README.md).
